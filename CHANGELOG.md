# 更新记录

本项目按语义化版本记录用户可见变更。

## [v0.5.10] - 2026-07-27

### 修复

- 修正 QQ 名片点赞对陌生人的成功提示：OneBot `send_like` 未报错时只能说明适配器接受了请求，不能证明对方资料卡实际收到。陌生人或好友关系未知时，插件和人设回复都会明确标记为“到账未核验”。
- 兼容部分适配器把 OneBot `retcode` 作为字符串返回的情况，非零字符串状态码不再被误判为成功。

### 说明

- QQ 的“允许陌生人点赞”只允许接收请求，不能解除 QQ 对机器人账号的静默风控。插件不会通过重复、拆分或延迟请求来规避该限制。

## [v0.5.9] - 2026-07-27

### 新增

- 新增独立的 `qq_like` 自动 QQ 名片点赞模块，默认关闭；支持“赞我”和“赞@用户”触发、私聊/群聊开关、群白名单、冷却和单条消息目标数量限制。
- 可选使用当前 AstrBot 默认模型与人格生成点赞后的自然回复。点赞结果只以临时上下文交给本轮模型，不写入后续会话历史；本模块不注册 LLM 工具。

### 优化

- 默认每个目标仅调用一次 `send_like(times=10)`，不再像上游插件一样连续请求五次，降低陌生人点赞额度和风控触发概率。
- 调用 `get_friend_list` 缓存好友关系，并将陌生人权限、每日额度、适配器不支持等失败情形分别提示；QQ 的隐私设置和服务端额度仍无法由插件绕过。

## [v0.5.8] - 2026-07-26

### 修复

- 兼容 B 站官方字幕返回的 `bilivideo.com` CDN 地址，以及可信 HTTP 字幕地址；插件会先升级到 HTTPS 后再读取。
- 多语言字幕列表中遇到空地址或不可信地址时会继续尝试下一条可信字幕，而不是直接放弃全部官方字幕。
- 字幕 CDN 请求不再携带登录 Cookie，避免将登录凭据发送给字幕下载地址。

## [v0.5.7] - 2026-07-26

### 修复

- 修复管理员扫码登录成功后，yt-dlp 下载器仍只读取手填 Cookie / cookies.txt、未使用扫码凭据的问题。
- 下载、抽帧、Gemini 视频分析现在与 B 站网页/API 使用同一套按优先级选出的有效凭据，降低已登录视频仍收到 HTTP 412 的情况。

## [v0.5.6] - 2026-07-26

### 修复

- 修复部分 AstrBot 环境收到 B 站 Brotli（`Content-Encoding: br`）压缩 JSON 后未解压、被误报为“非 JSON 数据”的问题。
- 二维码请求现在明确只请求 `gzip` / `deflate`，并新增 `Brotli` 解压兜底，即使代理或中转仍返回 `br` 也能继续解析。

## [v0.5.5] - 2026-07-26

### 修复

- B 站二维码生成和扫码状态轮询遇到网页、空内容、HTTP 拒绝或其它非 JSON 回包时，现在会给出具体原因，不再笼统显示“无法识别的数据”。
- 默认会在这类异常后自动再尝试一次不使用系统 `HTTP_PROXY` / `HTTPS_PROXY` 的直连，兼容部分代理、中转或风控页面干扰；可通过 `bilibili_video.qr_login.direct_retry_on_invalid_response` 关闭。

## [v0.5.4] - 2026-07-26

### 修复

- B 站扫码登录命令改为独立的 `helper_bili_*` 命名空间：`/helper_bili_login`、`/helper_bili_login_status`、`/helper_bili_login_cancel` 和 `/helper_bili_logout`。
- 不再注册通用的 `/bili_login`、`/bili_logout` 及未加“助手”前缀的中文别名，避免与 `Soulter/astrbot_plugin_bilibili` 同时安装时抢占同一条指令。

## [v0.5.3] - 2026-07-26

### 新增

- 新增管理员 B 站扫码登录：私聊执行 `/bili_login` 后，插件发送二维码、轮询扫码确认，并自动保存获得的登录凭据。
- 新增 `/bili_login_status`、`/bili_login_cancel` 和 `/bili_logout`，分别用于检查状态、取消二维码和清除本插件扫码保存的凭据。
- 新增 `bilibili_video.qr_login` 子配置，可控制扫码命令、私聊限制、轮询间隔、等待超时，以及扫码凭据与手工 Cookie 的优先级。

### 安全与优化

- 二维码请求不会携带已有 Cookie；只有取得 `SESSDATA` 才会落盘保存，且保存文件会在支持的系统上限制为当前用户可读写。
- 扫码成功后立即调用 B 站导航接口确认登录状态；登出会先结束正在运行的扫码任务，避免凭据被并发写回。

## [v0.5.2] - 2026-07-26

### 修复

- 修复 QQ 分享的 `b23.tv` 短链可能因附带追踪参数或错误携带 B 站 Cookie 而返回 HTTP 400。
- 短链现在会先移除分享追踪参数，并使用不带 Cookie 的短链专用请求头；收到直接 BV/av 跳转后不再额外请求落地页。
- 短链 `GET 400` 时会额外尝试 `HEAD` 读取跳转地址，提高 B 站短链服务波动时的成功率。

### 新增

- 插件启动时会向 B 站导航接口检查已配置 Cookie 的登录状态，只记录“有效、未登录、暂时无法验证”等状态，不输出 Cookie 内容。

## [v0.5.1] - 2026-07-26

### 新增

- 默认模型方式新增可选的均匀抽帧识图，可把字幕/必剪转写与视频画面一起交给当前 AstrBot 视觉模型。
- 新增抽帧开关、帧数、最大宽度、JPEG 清晰度和总图片大小上限配置。
- 字幕或转写不可用时，开启抽帧可回退为仅依据画面资料分析。

### 优化

- 抽帧使用已有 ffmpeg 依赖并受视频时长、下载大小、处理超时和图片总大小限制保护。
- 自动解析注入的抽帧与 LLM 工具临时返回的抽帧都会在当前 Agent 回复结束前标记为不保存，不写入后续对话历史。
- 视频文字资料仍可缓存；抽帧图片不进入持久缓存，避免长期占用内存。

## [v0.5.0] - 2026-07-26

### 新增

- 新增 `understand_bilibili_video` LLM 工具。
- 支持从 B 站链接、BV号、av号、`b23.tv` 短链、分享文本、直接 QQ 小程序卡片和引用卡片识别视频。
- 新增两种互斥分析方式：
  - Gemini 下载视频后分析画面、声音和屏幕文字。
  - AstrBot 当前默认模型读取 B 站官方字幕；无字幕时可回退到必剪语音转写。
- 新增自动解析方式：跟随 AstrBot、看到视频就主动回复、关闭自动解析。
- 新增分 P、时长/大小/超时限制、Cookie、缓存、并发去重和临时文件清理。
- 新增 B 站解析、卡片提取、分 P、缓存、网络分块读取、字幕采样和 Gemini 响应单元测试。

### 优化

- 两种视频分析方式都只生成事实资料，最终回复继续使用当前会话人格、历史和默认模型。
- 视频事实只参与当前轮请求，不写入长期对话历史，避免大段字幕持续占用上下文。
- 超长字幕按开头、中段代表片段和结尾采样，避免只保留视频前半段。
- Gemini 文件在分析成功、失败或取消后都会尽力删除。
- 短链逐跳校验 B 站域名；必剪上传地址限制为 B 站域名并强制升级为 HTTPS。
- 网络响应改为完整分块读取并设置累计大小上限，避免把分块 JSON 误判为损坏。
- 重写 README，增加模式对比、配置说明、隐私边界和故障排查。

### 依赖

- 新增 `aiohttp>=3.9,<4`。
- 新增 `yt-dlp>=2024.1.0`。
- 新增 `imageio-ffmpeg>=0.4`。

### 致谢

- Gemini 视频分析参考 `YUMU1658/astrbot_plugin_qq_tools`。
- 字幕、下载和必剪转写参考 `storyAura/astrbot_plugin_biliVideo`。

## [v0.4.16] - 2026-07-26

- 新增被引用 QQ 小程序、哔哩哔哩分享、网易云音乐及其它结构化卡片读取。
- 修复 GitHub API 发布时中文文件被错误编码，恢复 UTF-8 元数据和说明文档。

## [v0.4.x] - 2026-07

- 新增引用自身图片来源标记，在保留 LLM 识图的同时说明图片不是当前用户上传。
- 修复唤醒词缀普通消息屏蔽、防抖绕过和动态指令进入 LLM 的问题。
- 新增按 cron 从本地头像池自动更换 Bot QQ 头像。
- 新增 Steam 纯数字 AppID 的可配置触发方式。
- 新增 wakepro 默认屏蔽词迁移，并允许用户在初始化后自由删改。
- 新增本地多壁纸图库、管理员存图/删图和自动创建图库。

## [v0.3.x 及更早]

- 整合 QQ 头像/群成员/综合资料、Anime1、收款码、随机语音、Steam、Bot QQ 资料和唤醒增强模块。
