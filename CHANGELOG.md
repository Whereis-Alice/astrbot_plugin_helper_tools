# 更新记录

本项目按语义化版本记录用户可见变更。

## [v1.0.1] - 2026-08-29

### 优化

- 重做插件图标。原先的「圆角控制面板加几何化的『工』字」换成一把 45° 斜置的两用扳手：右上是梅花头（闭口环加六角套筒），左下是开口头（U 形卡口），中间柄沿 45° 轴一笔连成，不再套外层容器背板，改为单色渐变，16px 下仍可辨识。
- 图标的深色与浅色两套配色都取自控制台主题，默认跟随系统外观自动切换，控制台顶栏内联使用时随顶栏主题切换；控制台顶栏与 README 中的图标展示和说明同步更新。

## [v1.0.0] - 2026-08-29

### 说明

- 版本号从 `v0.12.0` 直接进到 `v1.0.0`。功能面已经稳定了一段时间（QQ / OneBot 兼容层、壁纸库、控制台、LLM 工具都成型），这一版又同时带来了控制台框架重做、默认配置行为修正和一处破坏性变更，按语义化版本该走大版本。
- 破坏性变更：壁纸库的「删除配置和磁盘文件」不再递归删除整个图库目录。现在只删除该图库索引到的图片，非图片文件一律保留，只有因删图而变空的目录会被回收；配置为非递归（`recursive: false`）的图库，其子目录内的图片不在索引范围内，因此也不再被删除。如果你之前是拿这个按钮当「清空整个目录」用的，现在需要自己在文件管理器里删。
- 行为变更：`wake.admin_wake_words` 的内置默认值 `宝贝`、`宝宝` 已移除。升级后如果你确实要用管理员专属唤醒词，请到控制台自行填写。

### 修复

- 修复列表类配置「清空后又自己长回来」的问题。此前 `read_list()` 读到空列表会静默回落到内置默认值，用户在控制台里删干净、保存、刷新，词表又原样出现，无法真正关掉对应触发方式。
  - `wake.admin_wake_words` 默认值由 `["宝贝", "宝宝"]` 改为空。这是本次最直接的用户可见问题：默认「自由触发」（消息里包含即命中）加上默认开启的唤醒增强，管理员随口说出这两个词就会唤醒模型，而且清空后关不掉。
  - `voice.trigger_keywords`、`qq_like.trigger_phrases`、`qq_like.mention_trigger_prefixes`、`anime1.update_times` 同样改为可清空并保持为空，分别表示不启用关键词自动触发、不响应「赞我」类消息、不响应「赞 @某人」、不定时检查更新。
  - `anime1.update_times` 填入非法值（例如 `99`、`abc`）时不再被强行拉回凌晨 1 点。
  - `steam.command_prefixes` 经确认属于「至少要有一个」的必需项，行为保持不变（清空仍回落到 `steam`）；想彻底关掉可配置查询指令请改用 `steam.commands_enabled` 开关。配置项说明已补上这一区别。
- `_conf_schema.json` 中一批带非空默认值的列表项补充了「留空会使用内置默认值」或「留空表示不启用」的说明，避免继续在界面上看不出区别。

### 安全

- 收窄「删除图库并删除磁盘文件」的爆炸半径。此前该操作对图库目录整体递归删除，如果把图库路径设成了混放目录（例如素材盘、甚至用户主目录），确认后目录里的文档、压缩包、子目录会一并不可逆消失。现在只删除该图库实际索引到的图片文件，并逐个复核「不是符号链接、解析后仍在图库根内、扩展名仍在白名单内、确实是文件」，随后只回收因删图而变空的目录，删除结果会明确回报删了多少张、失败多少张、保留了多少个非图片文件。
- 上述操作的目录校验新增拒绝用户主目录（此前只拒绝文件系统根、插件数据目录、`wallpapers` 总目录、符号链接和与其它图库重叠的路径）。
- 修复壁纸「已发送图片」索引非原子写的问题。此前直接覆盖写目标文件，进程被杀或磁盘写满时会留下半截 JSON，而读取端对解析失败是静默返回空表，结果是「撤回消息时同步删除图片」从此静默失效且没有任何提示。现在改为写临时文件再原子替换，读到的永远是完整的旧版本或完整的新版本。
- `twitter.proxy` 的说明中补充提示：该字段不是密钥字段、控制台会原样回显，不要把账号密码写进代理 URL。

### 优化

- 辅助工具控制台整体换框架：由「左侧竖向侧栏 + 右内容区」改成「顶栏 + 横向标签栏 + 单一滚动内容区 + 底部状态栏」。整页固定一个视口高度，只有内容区滚动，顶栏的主题切换、刷新、保存配置和底部状态信息始终可见，长表单不必滚回顶部才能保存。
- 标签栏新增数字角标：模块配置显示已启用模块数、运行记录显示今日记录数、壁纸库显示已索引图片数，为 0 或暂无数据时不显示。
- 控制台配色重做为深青绿主色调，圆角、分隔线、状态色和等宽数值排版统一，浅色主题同步整套重配，不再出现深色残留。顶栏与底部状态栏各显示一次插件版本号。
- 新增插件图标 `pages/helper-tools/logo.svg` 与紧凑版 `pages/helper-tools/logo-mark.svg`：圆角控制面板配几何化的「工」字，兼顾「工具」与「工字梁（承重的辅助结构）」两层含义，深浅两套配色，控制台顶栏内联使用。
- 壁纸库删除相关的界面文案与新行为对齐：确认框改为说明「只删除被索引到的图片、其它文件保留、只回收变空的目录」，对话框标题与按钮同步改为「删除图库图片」「删除配置和图片」。

## [v0.11.1] - 2026-08-27

### 修复

- 修复自动换头像在 AstrBot 与 OneBot 协议端分开部署时必然失败的问题：协议端解析本地路径用的是它自己容器里的文件系统，插件传过去的头像池路径在对端并不存在。现在按路径设置失败后会自动把图片读成 base64 再重试一次，头像池放在 AstrBot 侧也能正常轮换。
- 修复插件内部版本号没有跟着版本发布一起更新的问题，`main.py` 注册时上报的版本此前一直停在 `0.10.16`，与 `metadata.yaml` 不一致。

### 优化

- 补充 LLOneBot / LuckyLilliaBot 动作覆盖说明：插件用到的 20 个 OneBot 动作全部在该协议端已注册，README 兼容矩阵同步标注头像的 base64 兜底行为。

## [v0.11.0] - 2026-08-27

### 新增

- QQ 相关功能改用统一的 OneBot v11 兼容层：插件会自动识别当前协议端实现（LLOneBot/LuckyLilliaBot、NapCat、Lagrange、go-cqhttp 等），同一个功能按多个候选接口依次尝试，并记住第一个成功的调用方式，之后不再重复试错。
- 新增 `general.onebot_platform_names` 配置。如果你的协议端在 AstrBot 里注册成了非 `aiocqhttp` 的自定义平台名，把平台名填进来，戳一戳、QQ 资料、点赞、群历史等 QQ 功能就会对它生效。
- QQ 平台识别范围扩大：除 `aiocqhttp` 外，还会自动认出 `onebot`、`onebot_v11`、`llonebot`、`llbot`、`luckylilliabot`、`napcat`、`lagrange`、`gocqhttp`、`shamrock` 等平台名。
- 今日小猪新增 `rollpig.font_bold_file` 与 `rollpig.font_regular_file`，可指定自定义字体文件；留空时按内置字体、系统字体、PIL 默认字体顺序查找。
- README 新增“OneBot 协议端兼容性”一节，含功能兼容矩阵、LLOneBot / LuckyLilliaBot 接入提示，以及 QQ 指令报错的排查步骤。

### 修复

- 修复在 LLOneBot 上戳一戳发不出去的问题：不再调用该协议端并不存在的接口，改用它实际支持的群内戳和好友戳。
- 修复 LLOneBot 的戳一戳事件收不到或误判的问题：放宽事件类型识别、过滤撤回类通知，并兼容它把附加信息放在数组里的返回格式；触发者和被戳对象的账号识别增加多级回退。
- 修复设置 Bot 签名在 LLOneBot 上直接失败的问题，改走该协议端支持的个性签名字段，并同时回传当前昵称，避免签名改完后昵称被清空。
- 修复设置在线状态在 LLOneBot 上失败的问题：改用它支持的状态接口，并补齐该接口要求的全部必填参数。
- 修复名片点赞在 LLOneBot 上把“协议端没有这个接口”误报成“对方拒绝点赞”的问题；同时去掉该协议端不接受的多余参数。
- 修复群聊历史 OneBot 回填在 LLOneBot 上取不到消息的问题，自动改用它认识的参数写法。
- 修复图片和文件解析只按 go-cqhttp 返回键读取，导致 LLOneBot 上取不到原图或文件信息的问题；查询失败时会优雅降级，不再中断处理。
- 防撤回、引用识别和存图按消息 ID 查原消息时，若协议端消息缓存已过期（LLOneBot 默认 120 秒），改为静默降级，不再报错。
- 设置昵称、签名、在线状态和头像四条管理员命令失败时，返回带原因和错误码的中文提示，不再抛出原始异常。
- 修复以 HTTP 方式接入的协议端在动作失败时被当成成功的问题：协议端不抛异常、只在返回体里写 `status: failed` 时，插件现在也能识别失败原因、按需换用其他接口，并把真实错误码回报给用户。
- 修复群聊历史回填时把消息序号 0 当成“没有序号”的问题，避免从群聊最早的消息开始翻页时漏掉一页。

### 优化

- 头像自动更换服务的后台任务会被正确持有并在停用时取消等待，文件读写全部移出事件循环。
- 壁纸、Anime1 和今日小猪的缓存、状态与记录读写改为异步，避免大图库或大缓存阻塞消息处理。
- 今日小猪字体查找重写为“配置项 → 插件内置字体 → 系统字体 → PIL 默认”，缺字体只提示一次，不再反复告警。
- 时间戳展示统一带时区。
- 唤醒增强和壁纸模块原先被静默吞掉的异常改为带堆栈的告警日志，便于定位偶发问题。
- 人格头像缓存改为异步写入，缓存失败时提示头像已换但下次同步人格不会自动换回，不再静默丢失。
- 头像自动更换在非 OneBot 平台的事件上不再误取协议端接口，换头像失败会返回带错误码的中文提示。
- 自动化测试从 220 项增加到 360 余项，新增覆盖 OneBot 兼容层与返回体失败识别、Bot 资料管理、名片点赞、群历史回填与序号提取、防撤回、引用媒体识别、壁纸、Anime1、头像自动更换和唤醒增强。

## [v0.10.16] - 2026-08-26

### 修复

- 修复 WebUI 新增或自动创建图库后，点击全局“保存配置”会被页面打开时的旧图库列表覆盖，导致图库消失的问题。
- 图库接口现在返回服务器当前的完整配置条目；专用图库管理器、自动建库和通用模块配置会保持同步。
- 全局保存前会再次核对最新图库配置，避免旧页面或并行操作覆盖已经新增的图库。

## [v0.10.15] - 2026-08-20

### 修复

- 修复 bot 自己或其它插件发出后立即撤回的消息，偶发来不及进入防撤回缓存的问题。模块现在会先登记消息记录；图片快照仍在下载时收到撤回通知，也能至少恢复原消息内容。
- 对当前 OneBot bot 实例增加撤回前捕获：其它插件调用 `delete_msg`、`call_action("delete_msg")` 或 `bot.api.call_action("delete_msg")` 时，防撤回会在删除前通过 `get_msg` 读取并保存原消息。`astrbot_plugin_qqadmin` 的批量撤回和引用撤回因此可以恢复 bot 自己发出的文本与图片。
- 捕获钩子只在 QQ 群消息或群撤回事件上安装，并会在插件卸载时恢复协议端原方法，不影响非 QQ 平台或其它插件的正常调用。

## [v0.10.14] - 2026-08-13

### 修复

- 修复 AstrBot 内嵌控制台中点击壁纸图片“删除”后没有反应的问题。原实现依赖浏览器原生 `window.confirm()`，在部分 iframe 权限环境中会被直接拦截；现在改为控制台自己的站内确认对话框。
- “只删除图库配置”、清空运行记录、清除已上传配置文件和放弃未保存更改也统一使用站内确认框，避免相同环境下出现按钮无响应。
- 删除图片的确认文案明确说明会同步删除磁盘文件；确认后仍调用原有受鉴权的删除接口，并刷新图库数量、图片墙与缩略图缓存。

## [v0.10.13] - 2026-08-12

### 修复

- 修复 QQ 图片消息虽然被防撤回模块记录、但图片快照为 `cached_images=0`，撤回后只收到空消息或图片占位的问题。缓存阶段现在会同时读取 OneBot 原始图片段和 AstrBot 已转换的 `Image` 组件，并立即保存原图。
- QQ 图片下载增加上游实践使用的 Qzone `Referer`；原始引用失效时，会按顺序尝试 AstrBot 图片组件、同一消息的 `get_msg` 新引用，以及 `get_image`、`get_file` 等 OneBot 兼容参数。
- 图片快照失败从仅写 DEBUG 改为可见 WARN，并隐藏完整图片 URL、签名参数和 Base64。撤回通知日志改为 `restored_images=实际恢复数/原图数`，不再把“通知发送成功”表述成“图片恢复成功”。

## [v0.10.12] - 2026-08-11

### 新增与修复

- QQ 防撤回现在会在原消息到达时短期缓存图片原图，撤回后通过 OneBot `base64://` 图片段恢复，不再依赖可能已经失效的 QQ 临时图片 URL、文件标识或 AstrBot 与 QQ 协议端之间的共享文件路径。
- 撤回说明、原文字和恢复出的图片仍作为一条 QQ 消息发送；完整混合消息被适配器拒绝时，会再次尝试发送“文字说明 + 已缓存图片”，最后才降级为纯文字。
- 图片快照随防撤回消息缓存一同过期、淘汰和清理，旧版本留下的纯 JSON 缓存仍可正常读取；同时兼容 OneBot `data` 与 `result` 响应包装。

## [v0.10.11] - 2026-08-10

### 修复

- 修复引用其它插件自动发送的图片时，适配器提供的发送者信息不可靠导致图片被模型当成当前用户上传的问题。带消息 ID 的引用现在默认以 OneBot `get_msg` 原消息核对发送者和图片段，并兼容常见响应包装与带平台前缀的 QQ 号。
- 将引用图片来源说明优先注入本轮系统提示，图片仍保留并传给视觉模型，说明不会写入后续聊天历史。

## [v0.10.10] - 2026-08-10

### 优化

- 压缩环境感知中的当前发言者群内资料格式，移除重复的“当前发言者的”前缀，改为 `QQ=`、`QQ昵称=`、`群身份=`、`群等级=`、`群头衔=` 等紧凑字段，减少每轮 LLM 上下文占用。

## [v0.10.9] - 2026-08-10

### 新增

- 环境感知新增可选的当前发言者群内资料。开启后，QQ 群聊中的模型可准确获知当前发言者的 QQ 号、QQ昵称、群昵称、群身份、群等级、专属头衔、头衔到期和禁言到期信息；默认关闭，并以短缓存减少 OneBot 查询。

## [v0.10.8] - 2026-08-10

### 新增

- `get_qq_group_info` 新增群头像支持。默认会返回 QQ 公开群头像 URL，并在视觉模型可用时下载头像图片供模型查看；可通过工具参数 `include_avatar`、`return_image` 或 `qq_member.group_info_include_avatar` 配置关闭。

## [v0.10.7] - 2026-08-10

### 新增

- 环境感知新增 Bot 自己的当前群身份信息：在 QQ/OneBot 群聊中，模型可知道自己是普通群员、管理员还是群主，并可读取自己的群昵称、群等级、群专属头衔、头衔到期时间和禁言到期时间。

### 优化

- 新增 `perception.include_bot_group_identity` 和 `perception.bot_group_identity_cache_seconds` 配置。前者默认开启，后者默认缓存 60 秒以减少 OneBot 请求；私聊、非 QQ 平台或不支持群成员查询的适配器会自动跳过，不影响正常回复。

## [v0.10.6] - 2026-08-10

### 新增

- 新增 `get_qq_group_member_list` LLM 工具：读取 QQ 群成员列表，提供群名称、QQ号、QQ昵称、群昵称、群身份、群等级、专属头衔和 OneBot 额外字段；支持关键词筛选、分页和单次输出上限，避免大群资料挤满模型上下文。
- 新增 `get_qq_group_info` LLM 工具：读取群名称、群号、备注、建群时间、等级、人数和上限；可选补充成员身份/管理员/头衔统计、群荣誉和当前 Bot 账号的 `@全体成员` 配额。

### 优化

- QQ 群资料调用兼容常见 OneBot 直接返回与 `data`/`result` 包装返回；荣誉或 `@全体` 接口不被当前适配器支持时，会保留已有群详情并给出降级说明。
- 扩充 `qq_member` 配置项，支持调整成员列表默认条数、单次硬上限、文本上限，以及群详情的统计、荣誉和 `@全体` 信息开关。

## [v0.10.5] - 2026-08-09

### 修复与优化

- 将控制台桌面侧栏从 268px 收窄为 216px，同步压紧品牌、导航和间距，把更多横向空间留给图库和其它工作页面。
- 重排侧栏底部：连接状态与刷新合并到标题行，主题切换保持为紧凑分段控件，保存配置改为单行操作；侧栏固定为视口高度，底部操作不会再被长页面内容推到页面底部。
- 保持窄屏原有横向导航行为，自动取消桌面侧栏的固定高度，避免手机端出现额外滚动或溢出。

## [v0.10.4] - 2026-08-09

### 修复与优化

- 再次重做“壁纸库”页面的信息层级：移除图片墙上方常驻的大型统计卡片和图库列表，将完整统计、全部图库、新增、编辑及两种删除方式收进“管理图库”弹窗。
- 将图库选择、状态、管理、刷新、上传、搜索和排序压缩为一条桌面工具栏，图片墙从首屏约 16% 的位置开始显示，避免管理信息挤占主要浏览区域。
- 优化手机端：隐藏重复页标题，上传和筛选默认折叠为一个入口；展开后仍可完整上传、搜索和排序，且双列图片墙不会产生横向溢出。

## [v0.10.3] - 2026-08-09

### 修复与优化

- 修复“壁纸库”页面中缩略图无法显示的问题。缩略图和预览改为通过 AstrBot 插件页的已鉴权桥接接口读取，不再让 iframe 直接请求无凭据的图片 URL；原图下载和图片上传同样走桥接通道。
- 重做壁纸库页面布局：当前图库改为顶部选择栏，全部图库收进可展开管理区，默认将主要宽度留给更大的全宽图片墙；图片缩略图会限制并发加载，避免大量图片同时占用后台。
- 新增两种删图库方式：可仅删除配置并保留磁盘文件，也可在输入图库名称二次确认后删除配置和图库目录中的全部文件。物理删除前会拒绝高风险目录、符号链接及与其它图库重叠的路径，并同步清理已发送壁纸的本地记录。
- 保留原有二进制缩略图、文件预览和批量上传接口，避免影响已有外部调用；新增接口均有测试覆盖。

## [v0.10.2] - 2026-08-09

### 新增

- 辅助工具控制台新增独立“壁纸库”页：可查看全部图库的图片数量、占用、目录状态和递归扫描状态，并浏览缩略图、路径、尺寸、格式、帧数、大小和修改时间。
- 支持在控制台新增、编辑、删除图库配置，以及批量上传、搜索、排序、预览、下载、重命名和删除壁纸图片。目录不存在时可自动创建；删除图库配置不会删除磁盘图片。

### 修复与优化

- 图库的文件操作只接受“图库标识 + 相对路径”，拒绝绝对路径、目录穿越和符号链接；上传会校验实际图片格式、像素、单图大小、批次大小和现有去重规则，缩略图不会直接返回原图。
- 图片重命名或删除会同步已发送壁纸的本地消息记录，避免后续引用图片执行删图时指向旧路径。
- 多文件上传在读取请求体时就限制单图和总大小，避免异常大批次图片占用过多内存。

## [v0.10.1] - 2026-08-09

### 修复

- 修复唤醒增强的“屏蔽已知 QQ 机器人账号”可能在其它插件改写事件发送者信息后误拦截普通用户的问题。现在会在异步处理前固定判断，并优先读取 OneBot 原始事件的真实 `user_id`。

## [v0.10.0] - 2026-08-09

### 新增

- 新增 AstrBot 原生插件页“辅助工具控制台”：在同一个页面查看模块开关、已注册的 LLM 工具、本地数据占用、服务状态和隐私安全的运行记录。
- 配置页直接按 `_conf_schema.json` 自动生成，支持模块搜索、只看已启用模块、一键展开/收起、模板列表和受控文件上传；后续新增模块与字段会自动进入控制台。
- 新增本地活动记录，可查看重要模块动作、结果、时间和匿名会话类型；默认不记录聊天正文、图片、语音、链接、Cookie、Token 或 API Key。
- 新增存储诊断，统计插件数据目录中的文件数量、占用和分目录大小，并显示 B 站凭据、扫码任务、历史库、壁纸图库、自动换头像和 LLM 工具状态。

### 修复与优化

- 保存相关模块配置后，会立即同步本插件已注册 LLM 工具的可用状态，无需为了普通工具开关重载插件。
- 控制台读取的 LLM 工具状态以实际注册工具实例为准，避免仅根据配置预测造成显示不一致。
- 清空运行记录后不再自动写回一条“已清空”记录；关闭会话标识显示后，也不会继续把旧记录中的群号或用户号回传给页面。
- 上传或清除配置文件前会检查是否存在未保存的表单修改，避免重新加载页面时丢失其它配置。

## [v0.9.12] - 2026-08-08

### 修复与优化

- 修复只 `@ bot`、仅发送一个唤醒词，或仅发送 AstrBot 全局唤醒词缀时，AstrBot 因当前消息为空而跳过 LLM 请求的问题。
- 新增 `wake.empty_wake_response_enabled` 和 `wake.empty_wake_prompt`：可分别控制无文字唤醒是否回复，以及本轮交给当前人设的提示词。
- 单独发送完整唤醒词会继续遵守自由、前缀、后缀触发方式；为 LLM 临时注入的提示不会写入后续聊天历史，也不会提前影响其它插件命令。

## [v0.9.11] - 2026-08-04

### 修复

- 修复将 Nitter 的 `/pic` 图片代理 URL 误识别为账号 `pic`，导致查询最近推文时请求错误路径并收到 404 的问题。
- 纯文本账号名仍可正常查询；只有 Nitter URL 中的静态资源和系统路由会被排除。

## [v0.9.10] - 2026-08-02

### 修复

- 修复防撤回重发带 QQ 引用的消息时继续携带原 `reply` 消息 ID，导致接收端显示“原消息已过期”的问题。
- 重发时将活动引用转换为静态“【引用消息】”文本，同时保留正文、图片、表情等原消息内容。

## [v0.9.9] - 2026-07-31

### 修复

- 修复戳一戳随机调用其它插件命令时，部分依赖事件发送者 QQ 号或昵称的指令拿到 Bot 自己参数的问题。
- 下游命令现在读取原始戳一戳用户的身份，同时继续保留内部命令标记、群聊历史跳过和 Agent 临时消息保护，避免重新把内部命令写成用户发言。

## [v0.9.8] - 2026-07-31

### 修复

- 修复防撤回把撤回说明和原消息拆成两条普通消息发送的问题。
- 现在会尽量把说明和原消息段组合成同一条 QQ 消息；适配器拒绝混合消息时降级为同一条文字说明。

## [v0.9.7] - 2026-07-31

### 新增

- 新增管理员命令 `/戳命令列表`，并提供 `/导出戳命令`、`/随机命令列表` 别名。
- 导出当前实际可随机调用的命令池，一行一个命令；自动移除 AstrBot 唤醒词缀并去重，方便复制备份和重新组合配置。

## [v0.9.6] - 2026-07-30

### 修复

- 修复群聊戳一戳触发人设回复时，模型可能把历史中其它成员的发言误认为当前戳一戳用户发言的问题。
- 保留群聊历史用于延续话题，同时在当前轮临时注入本次触发者的 QQ 号、昵称和身份边界说明；该说明不会写入后续上下文，也不会影响普通消息或其它插件命令。

## [v0.9.5] - 2026-07-30

### 修复与优化

- 优化群聊历史卡片：改为只截取卡片元素本身，去除固定渲染视口造成的底部空白，并收紧单条记录的排版间距。

## [v0.9.4] - 2026-07-30

### 新增

- 新增默认关闭的 `anti_revoke` QQ 防撤回模块，支持按群监控、发送者/操作者忽略名单、默认私聊/群聊通知目标、按群目标和管理员管理命令。
- 防撤回优先缓存 OneBot 原始消息段，撤回后可恢复文本、图片、语音、视频、文件、卡片和合并转发；适配器拒绝原消息时自动降级为文字说明。

### 修复与优化

- 不再依赖上游旧版的单一 `event.message_obj.raw_message` 位置和旧消息类型判断，兼容新版 AstrBot 的事件包装，并增加内存缓存、磁盘兜底、大小限制和过期清理。
- 为戳一戳的“调用其它插件命令”增加后台日志：记录选中的命令、目标、会话、入队结果，以及下游插件异常时的失败原因。
- 更新戳一戳人设回复和禁言失败回复的默认提示词，优先延续当前上下文，并明确禁言失败的真实原因。
- 重新整理配置页顺序：防撤回紧跟环境感知，消息路由、引用识别、上下文理解、QQ 工具和主动动作分组显示。

### 致谢

- QQ 防撤回的产品场景参考 `Foolllll-J/astrbot_plugin_anti_revoke`。该上游采用 AGPL-3.0，本模块为兼容本 MIT 仓库的独立实现，没有复制或并入上游源码。

## [v0.9.3] - 2026-07-30

### 修复

- 支持 B 站新版专栏分享链接跳转到 `/opus/<数字>` 的情况，包括现代专栏正文容器、作者和顶部封面提取。
- 修复新版专栏短链被视频模块误判为视频的问题。

## [v0.9.2] - 2026-07-30

### 修复

- 修复引用使用 `b23.tv` 短链的 B 站专栏卡片时，视频模块误把短链当成视频解析，产生“短链经过多次跳转后仍无法识别”的失败资料和日志的问题。
- 专栏模块现在会先确认短链最终页面类型；确认是专栏后跳过视频处理，确认是视频时仍交给视频模块解析。

## [v0.9.1] - 2026-07-30

### 新增

- 新增 `bilibili_article` 模块：自动识别 B 站专栏链接、短链和被引用的 QQ 专栏卡片。
- 优先使用 B 站官方专栏接口读取标题、作者、摘要和正文，接口不可用时回退专栏网页；可选把封面作为当前轮视觉资料交给默认模型。
- 新增 `bilibili_article.max_article_chars` 正文长度限制，超长正文保留开头和结尾；网络响应、封面大小、请求超时和缓存条目也都可配置。

### 修复与优化

- 专栏正文、封面和外部资料安全说明只注入当前请求，并在 Agent 完成后标记为不保存，避免图片和正文污染后续上下文。
- B 站短链只有在最终跳转到专栏时才进入专栏解析，不会把普通视频短链误当成专栏。

### 致谢

- B 站专栏卡片字段和引用场景参考 AstrBot 社区插件的通用实现思路；本模块为独立实现。

## [v0.9.0] - 2026-07-30

### 新增

- 新增默认关闭的 `poke` 戳一戳互动模块。支持被戳监听、用户/群冷却、跟戳、按权重反戳、当前人设回复、QQ 表情、本地图片、语音、可选禁言和随机调用其它插件命令。
- 新增 `/戳 @用户 [次数]`、`/戳我` 和管理员专用的 `/戳全体成员`；新增 `poke_qq_user` LLM 工具，让当前模型可在次数上限内主动戳明确的 QQ 用户。
- 新增关键词主动戳和基于 AstrBot 内置 Cron 管理器的定时戳。两项默认关闭，定时目标默认为空，不包含任何预置群号或 QQ 号。

### 修复与优化

- 修复上游随机调用 `怒撕` 等命令时沿用原戳一戳用户身份，导致内部命令被日志、聊天记录或后续 Agent 当作用户发言的问题。合成命令现在以 Bot 自身为作者、保留原用户为 @ 目标、跳过本地历史，并将显式 Agent 回合标为不持久化；被戳人设回复的内部提示和回答同样只保留当前轮。
- 兼容 AstrBot 开启“忽略机器人自身消息”的情况：仅在唤醒检查阶段使用传输身份，进入插件处理器后立即恢复 Bot 作者；随机插件命令不会额外进入默认 LLM。
- 随机命令仅在群聊参与候选，避免 OneBot 私聊按发送者 ID 路由时把结果发给 Bot 自己；空图片、语音或命令池会自动退出候选。
- 主动戳次数、全员目标数和发送间隔均有边界；禁言前检查 Bot 与目标群身份；定时任务复用 AstrBot 内置 Cron，不再引入独立调度器。

### 致谢

- 产品场景参考 `Zhalslar/astrbot_plugin_pokepro`。该上游采用 GPL-3.0，本模块为适配本 MIT 仓库的独立实现，没有复制或并入上游源码。

## [v0.8.5] - 2026-07-30

### 修复

- 修复 NapCat/OneBot 已将 X/Twitter 图片送达 QQ、但返回 `retcode=1200` 的 `sendMsg` 回执超时时，LLM 工具丢弃正常检索结果并误报整体失败的问题。现在会把该情况标记为“图片可能已经发出”，保留推文资料，并明确禁止重试以避免重复图片；其他发送异常仍会正常抛出。

## [v0.8.4] - 2026-07-30

### 修复

- 修复部分 X/Twitter 搜索结果缺少结构化 `reposted_by`、以“目标账号作为作者 + `RT @原作者:` 正文”返回时，被错误标记为本人发布的问题。现在会识别正文开头的原生转推格式，还原转推账号和原作者，并在关闭默认包含转推时正确排除。
- 修复只有用户名、没有显示名时作者归属标签重复显示用户名的问题；正文中间普通讨论 `RT @...` 不会被当成转推。

## [v0.8.3] - 2026-07-30

### 修复

- 修复关闭默认包含转推后，最近候选动态全是转推时直接返回空结果的问题。账号最近动态和推文搜索现在会在转推、R18 文字或平台敏感标记过滤后自动读取更早结果，尽量补满最终返回条数。

### 优化

- Nitter 会安全跟随同一实例、同一路径的“加载更多”游标，FxTwitter 会使用响应中的底部游标；两者都会对推文和游标去重，后续页失败时保留已取得的结果。
- 新增 `filtered_result_max_pages` 和 `filtered_result_max_candidates` 配置，默认最多读取 6 页、扫描 120 条候选，避免连续大量转推时无限查询。

## [v0.8.2] - 2026-07-30

### 修复

- 修复 X/Twitter 图片下载只读取一次网络流，连接分块时可能保存不完整 JPEG、在 QQ 中显示大片灰块的问题。现在会逐块读取到 EOF，并用 Pillow 校验文件结构和实际解码；临时传输错误、HTTP 5xx、限流或图片完整性失败会按配置重试，仍损坏则跳过发送。
- 修复账号最近动态和 `from:用户名` 搜索无法区分本人发布与转推的问题。现在解析 FxTwitter `reposted_by`、Nitter 转推标记，并用目标账号与实际作者交叉校验。

### 优化

- 新增 `include_reposts_by_default` 配置，默认关闭；`get_x_recent_posts` 和 `search_x_posts` 新增 `include_reposts` 参数供模型按用户要求临时覆盖。查找画师本人作品时默认排除转推。
- 每条推文以及发送给模型或 QQ 的图片都会标明本人发布、转推账号和原作者，避免把转推图片错误归属给目标账号。

## [v0.8.1] - 2026-07-30

### 新增

- 环境感知新增 `log_mode` 配置，可选择“关闭”“仅记录已注入”或“记录完整内容”。默认只记录会话标识和感知文本长度；完整模式会记录本轮实际注入的感知文本，并在配置页明确提示会话标识及完整内容可能包含群号、群名或发送者 QQ 号。

## [v0.8.0] - 2026-07-30

### 新增

- 新增默认关闭的 `perception` 环境感知模块。可按配置向当前 LLM 请求附加准确时间和时段、中国法定节假日/调休、农历、节气、可选黄历、平台、群名和媒体类型；可选附加当前发言者的真实 QQ 号，避免只依赖正文自称判断身份。
- 新增默认关闭的 `chat_history` 当前群聊历史检索模块，注册 `search_current_group_chat_history` LLM 工具。支持关键词、时间范围、发送者、数量和分页，只允许查询当前 QQ 群，并可受允许/禁止群列表、回填页数和本地保留边界限制。
- 群聊历史检索支持可选的 AstrBot T2I HTML 摘要卡片，提供“夜航”“纸笺”“薄荷”“霓虹”四种皮肤；可按模型的 `render_card` 参数发送，或由配置启用自动发送。

### 修复与优化

- 修复引用 bot 或其它插件自动发送的图片时，引用段缺少发送者/图片链会被误认为当前用户上传的问题。现在会回查 OneBot `get_msg`，在消息阶段标注来源，并在 LLM 请求阶段再次补入仅本轮来源说明。
- 环境感知的节假日数据改为只使用 `chinese-calendar` 实际覆盖范围；库未覆盖的未来年份会明确提示，避免生成不准确的法定节假日结论。
- 群聊历史只存规范化文字和媒体占位符，不存图片/语音字节或完整 OneBot 原始报文。历史原文被标注为不可信资料，检索结果和卡片说明均不会进入后续会话历史。
- 新增 `chinese-calendar` 和 `lunar-python` 依赖，以及引用回查、节假日覆盖、QQ 身份感知、历史检索范围、OneBot 回填、T2I 卡片与临时上下文测试。

### 致谢

- 环境感知场景参考 `miaoxutao123/astrbot_plugin_LLMPerception`；当前群聊历史检索场景参考 `kawayiYokami/astrbot_plugin_angel_eye`。两个上游仓库均采用 AGPL-3.0；本次模块均为独立实现，没有复制或并入上游代码。

## [v0.7.0] - 2026-07-29

### 新增

- 新增默认关闭的 `twitter` X/Twitter 资料检索模块，注册 `find_x_account`、`get_x_post`、`get_x_recent_posts` 和 `search_x_posts` LLM 工具。模型可查找画师、VTuber、公司或个人账号，读取公开推文、最近动态与按需返回的图片，并继续按当前 AstrBot 人格自然回复。
- 新增 `helper_x_search`、`helper_x_account`、`helper_x_recent`、`helper_x_post` 命令，使用独立命名空间，避免与其它 X/Twitter 插件冲突。
- 支持 `x.com`、`twitter.com` 和已配置 Nitter 实例的推文链接自动解析；可选择跟随 AstrBot 唤醒规则、看到链接主动回复或关闭自动解析。
- 支持自建 Nitter 作为优先数据源，提供“自动（Nitter 失败回退 FxTwitter）”“仅 Nitter”“仅 FxTwitter”三种来源模式。

### 安全与优化

- 推文、账号简介、引用内容和媒体说明均作为不可信外部资料注入当前轮，不会覆盖 AstrBot 人格；自动注入的 X 图片与文字会在回复后排除出后续会话历史。
- 默认严格过滤数据源敏感标记和可编辑 R18 关键词；可选使用 OpenAI 兼容视觉模型审核每张待发送图片，审核失败或不明确时保守拦截。
- 自建 Nitter 的图片会先下载到 AstrBot 服务器后再发送，避免 QQ 客户端无法访问 `127.0.0.1`、Docker 内网地址或 Nitter 私有端口。
- 新增 `beautifulsoup4` 依赖，并加入 Nitter 页面解析、FxTwitter 回退、R18 过滤、临时上下文和命令命名空间测试。

### 致谢

- X/Twitter 功能的产品场景参考 `Ars1027/astrbot_plugin_twitter`。该上游项目采用 AGPL-3.0；本模块独立实现，没有复制或并入其代码。

## [v0.6.6] - 2026-07-29

### 优化

- 配置页的所有顶层模块标题统一使用 `【】` 标记，便于在较长的配置列表中快速辨认模块边界。

## [v0.6.5] - 2026-07-28

### 修复

- 修复部分第三方指令已经正常执行、却仍额外触发默认对话的问题。识别到命令后会在最终 LLM 请求阶段再次拦截，即使指令插件错误地将 `should_call_llm(False)` 写回事件也不会失效。
- 命令识别改为读取 AstrBot 当前注册的完整命令名和别名，支持多词命令、命令组、别名以及插件重载后的新命令。

## [v0.6.4] - 2026-07-28

### 修复

- 唤醒增强恢复为默认只处理群聊：私聊不再经过群聊用的消息防抖、唤醒冷却、屏蔽词、复读拦截或指令屏蔽，避免正常私聊偶发无回复。全局黑名单仍会在私聊中生效。
- 新增 `wake.apply_to_private_messages` 开关；只有明确开启后，私聊才使用完整的唤醒增强规则。
- 被唤醒增强主动停止的消息现在会记录不含正文的原因和会话标识，便于从日志判断是否为冷却、屏蔽词、复读或命令屏蔽所致。

## [v0.6.3] - 2026-07-27

### 修复

- B 站默认模型抽帧现在会先探测实际下载媒体的容器时长，再均匀采样，避免 B 站 API 时长与下载文件不一致时反复尝试文件末尾之外的时间点。
- 当 ffmpeg 成功结束但尾部时间点没有生成画面文件时，会停止后续无效采样并只记录一条汇总信息；已经成功的画面仍会正常交给当前模型。

## [v0.6.2] - 2026-07-27

### 新增

- 新增默认关闭的 `web_browser` 网页浏览模块，注册 `browse_webpage` LLM 工具。它使用 Playwright 读取公开网页标题、正文和可选截图，不提供点击、输入、登录、下载或表单提交。
- 网页结果和截图均只作为当前轮临时资料交给模型，Agent 完成后会从后续会话历史移除。

### 安全与优化

- 每次浏览使用新的无 Cookie 浏览器上下文；初始地址、重定向和页面资源请求都会校验协议、域名黑白名单、DNS 解析结果和本机/内网/保留 IP，默认拒绝私有网络访问。
- 限制页面正文长度、截图大小、并发数量和加载时间；页面中的提示词、指令或工具调用要求会明确作为不可信网页内容处理。

## [v0.6.1] - 2026-07-27

### 修复

- 修正 GitHub 发布包中今日小猪中文字体文件名的编码，确保 `rollpig` 卡片能加载内置字体并按预期渲染。

## [v0.6.0] - 2026-07-27

### 新增

- 新增可独立开关的 `rollpig` 今日小猪模块，支持 `/rollpig`、`/今日小猪`、`/抽小猪`、`/我的小猪` 命令；每日同一用户结果固定，次日按配置时区自动刷新。
- 内置并本地打包今日小猪素材库、图片和字体；可选上传自定义 JSON 素材库并指定自定义图片目录。

### 修复与优化

- 重写图片渲染尺寸和坐标处理，所有传入 Pillow 的值都会归一为整数，修复上游 `roll_pig` 在部分 Pillow/运行环境中出现的 `'float' object cannot be interpreted as an integer` 异常。
- 修复上游“查看被 @ 用户”失效的问题：现在直接读取真实 @ 消息组件，不再依赖不含 @ 的纯文本消息。
- 群聊 @ 目标、查看他人、管理员保护、时区和图片缓存保留时间均可单独配置。

## [v0.5.10] - 2026-07-27

### 修复

- 修正 QQ 名片点赞对陌生人的成功提示：OneBot `send_like` 未报错时只能说明适配器接受了请求，不能证明对方资料卡实际收到。陌生人或好友关系未知时，插件和人设回复都会明确标记为“到账未核验”。
- 兼容部分适配器把 OneBot `retcode` 作为字符串返回的情况，非零字符串状态码不再被误判为成功。

### 说明

- QQ 的“允许陌生人点赞”只允许接收请求，不能解除 QQ 对机器人账号的静默风控。插件不会通过重复、拆分或延迟请求来规避该限制。

## [v0.5.9] - 2026-07-27

### 新增

- 新增独立的 `qq_like` 自动 QQ 名片点赞模块，默认关闭；支持“赞我”和“赞@用户”触发、私聊/群聊开关、群白名单、冷却和单条消息目标数量限制。
- 可选使用当前 AstrBot 默认模型与人格生成点赞后的自然回复。点赞结果只以临时上下文交给本轮模型，不写入后续会话历史；本模块不注册 LLM 工具。

### 优化

- 默认每个目标仅调用一次 `send_like(times=10)`，不再像上游插件一样连续请求五次，降低陌生人点赞额度和风控触发概率。
- 调用 `get_friend_list` 缓存好友关系，并将陌生人权限、每日额度、适配器不支持等失败情形分别提示；QQ 的隐私设置和服务端额度仍无法由插件绕过。

## [v0.5.8] - 2026-07-26

### 修复

- 兼容 B 站官方字幕返回的 `bilivideo.com` CDN 地址，以及可信 HTTP 字幕地址；插件会先升级到 HTTPS 后再读取。
- 多语言字幕列表中遇到空地址或不可信地址时会继续尝试下一条可信字幕，而不是直接放弃全部官方字幕。
- 字幕 CDN 请求不再携带登录 Cookie，避免将登录凭据发送给字幕下载地址。

## [v0.5.7] - 2026-07-26

### 修复

- 修复管理员扫码登录成功后，yt-dlp 下载器仍只读取手填 Cookie / cookies.txt、未使用扫码凭据的问题。
- 下载、抽帧、Gemini 视频分析现在与 B 站网页/API 使用同一套按优先级选出的有效凭据，降低已登录视频仍收到 HTTP 412 的情况。

## [v0.5.6] - 2026-07-26

### 修复

- 修复部分 AstrBot 环境收到 B 站 Brotli（`Content-Encoding: br`）压缩 JSON 后未解压、被误报为“非 JSON 数据”的问题。
- 二维码请求现在明确只请求 `gzip` / `deflate`，并新增 `Brotli` 解压兜底，即使代理或中转仍返回 `br` 也能继续解析。

## [v0.5.5] - 2026-07-26

### 修复

- B 站二维码生成和扫码状态轮询遇到网页、空内容、HTTP 拒绝或其它非 JSON 回包时，现在会给出具体原因，不再笼统显示“无法识别的数据”。
- 默认会在这类异常后自动再尝试一次不使用系统 `HTTP_PROXY` / `HTTPS_PROXY` 的直连，兼容部分代理、中转或风控页面干扰；可通过 `bilibili_video.qr_login.direct_retry_on_invalid_response` 关闭。

## [v0.5.4] - 2026-07-26

### 修复

- B 站扫码登录命令改为独立的 `helper_bili_*` 命名空间：`/helper_bili_login`、`/helper_bili_login_status`、`/helper_bili_login_cancel` 和 `/helper_bili_logout`。
- 不再注册通用的 `/bili_login`、`/bili_logout` 及未加“助手”前缀的中文别名，避免与 `Soulter/astrbot_plugin_bilibili` 同时安装时抢占同一条指令。

## [v0.5.3] - 2026-07-26

### 新增

- 新增管理员 B 站扫码登录：私聊执行 `/bili_login` 后，插件发送二维码、轮询扫码确认，并自动保存获得的登录凭据。
- 新增 `/bili_login_status`、`/bili_login_cancel` 和 `/bili_logout`，分别用于检查状态、取消二维码和清除本插件扫码保存的凭据。
- 新增 `bilibili_video.qr_login` 子配置，可控制扫码命令、私聊限制、轮询间隔、等待超时，以及扫码凭据与手工 Cookie 的优先级。

### 安全与优化

- 二维码请求不会携带已有 Cookie；只有取得 `SESSDATA` 才会落盘保存，且保存文件会在支持的系统上限制为当前用户可读写。
- 扫码成功后立即调用 B 站导航接口确认登录状态；登出会先结束正在运行的扫码任务，避免凭据被并发写回。

## [v0.5.2] - 2026-07-26

### 修复

- 修复 QQ 分享的 `b23.tv` 短链可能因附带追踪参数或错误携带 B 站 Cookie 而返回 HTTP 400。
- 短链现在会先移除分享追踪参数，并使用不带 Cookie 的短链专用请求头；收到直接 BV/av 跳转后不再额外请求落地页。
- 短链 `GET 400` 时会额外尝试 `HEAD` 读取跳转地址，提高 B 站短链服务波动时的成功率。

### 新增

- 插件启动时会向 B 站导航接口检查已配置 Cookie 的登录状态，只记录“有效、未登录、暂时无法验证”等状态，不输出 Cookie 内容。

## [v0.5.1] - 2026-07-26

### 新增

- 默认模型方式新增可选的均匀抽帧识图，可把字幕/必剪转写与视频画面一起交给当前 AstrBot 视觉模型。
- 新增抽帧开关、帧数、最大宽度、JPEG 清晰度和总图片大小上限配置。
- 字幕或转写不可用时，开启抽帧可回退为仅依据画面资料分析。

### 优化

- 抽帧使用已有 ffmpeg 依赖并受视频时长、下载大小、处理超时和图片总大小限制保护。
- 自动解析注入的抽帧与 LLM 工具临时返回的抽帧都会在当前 Agent 回复结束前标记为不保存，不写入后续对话历史。
- 视频文字资料仍可缓存；抽帧图片不进入持久缓存，避免长期占用内存。

## [v0.5.0] - 2026-07-26

### 新增

- 新增 `understand_bilibili_video` LLM 工具。
- 支持从 B 站链接、BV号、av号、`b23.tv` 短链、分享文本、直接 QQ 小程序卡片和引用卡片识别视频。
- 新增两种互斥分析方式：
  - Gemini 下载视频后分析画面、声音和屏幕文字。
  - AstrBot 当前默认模型读取 B 站官方字幕；无字幕时可回退到必剪语音转写。
- 新增自动解析方式：跟随 AstrBot、看到视频就主动回复、关闭自动解析。
- 新增分 P、时长/大小/超时限制、Cookie、缓存、并发去重和临时文件清理。
- 新增 B 站解析、卡片提取、分 P、缓存、网络分块读取、字幕采样和 Gemini 响应单元测试。

### 优化

- 两种视频分析方式都只生成事实资料，最终回复继续使用当前会话人格、历史和默认模型。
- 视频事实只参与当前轮请求，不写入长期对话历史，避免大段字幕持续占用上下文。
- 超长字幕按开头、中段代表片段和结尾采样，避免只保留视频前半段。
- Gemini 文件在分析成功、失败或取消后都会尽力删除。
- 短链逐跳校验 B 站域名；必剪上传地址限制为 B 站域名并强制升级为 HTTPS。
- 网络响应改为完整分块读取并设置累计大小上限，避免把分块 JSON 误判为损坏。
- 重写 README，增加模式对比、配置说明、隐私边界和故障排查。

### 依赖

- 新增 `aiohttp>=3.9,<4`。
- 新增 `yt-dlp>=2024.1.0`。
- 新增 `imageio-ffmpeg>=0.4`。

### 致谢

- Gemini 视频分析参考 `YUMU1658/astrbot_plugin_qq_tools`。
- 字幕、下载和必剪转写参考 `storyAura/astrbot_plugin_biliVideo`。

## [v0.4.16] - 2026-07-26

- 新增被引用 QQ 小程序、哔哩哔哩分享、网易云音乐及其它结构化卡片读取。
- 修复 GitHub API 发布时中文文件被错误编码，恢复 UTF-8 元数据和说明文档。

## [v0.4.x] - 2026-07

- 新增引用自身图片来源标记，在保留 LLM 识图的同时说明图片不是当前用户上传。
- 修复唤醒词缀普通消息屏蔽、防抖绕过和动态指令进入 LLM 的问题。
- 新增按 cron 从本地头像池自动更换 Bot QQ 头像。
- 新增 Steam 纯数字 AppID 的可配置触发方式。
- 新增 wakepro 默认屏蔽词迁移，并允许用户在初始化后自由删改。
- 新增本地多壁纸图库、管理员存图/删图和自动创建图库。

## [v0.3.x 及更早]

- 整合 QQ 头像/群成员/综合资料、Anime1、收款码、随机语音、Steam、Bot QQ 资料和唤醒增强模块。
